# Render configuration for Zylix Storefront static site & backend
services:
  - type: static
    name: zylix
    buildCommand: npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
